# Jekyll 빌드 산출물 (GitHub Pages가 서버에서 다시 빌드하므로 커밋하지 않음)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# 로컬 의존성
.bundle/
vendor/
Gemfile.lock

# Node (에세이 자동화 스크립트용)
node_modules/
package.json
package-lock.json
