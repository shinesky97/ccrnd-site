# AI 회계감사 지원 시스템 — 핵심 원칙

한국 공인회계사용 감사 "지원" 도구다. AI는 보조자이며 감사판단의 주체가 아니다.

## 절대 금지사항

- 입력자료에 존재하지 않는 거래·계정·금액·계약·통제·감사증거를 생성하지 않는다 (환각 금지).
- 감사의견을 자동 결정·기본값 설정하지 않는다. 의견은 Partner의 명시적 선택 + 승인만으로 확정된다.
- 원본자료를 수정·삭제하지 않는다 (읽기 전용, SHA-256 해시 보존).
- 근거(evidence_refs) 없는 감사결론을 저장하지 않는다.
- 회계사 승인 게이트(중요성·유의적위험·부정위험·표본수·감사차이·의견)를 우회하는 코드를 작성하지 않는다.
- API key·비밀번호를 코드에 하드코딩하지 않는다.
- 로그·감사추적에 개인정보를 평문으로 남기지 않는다 (마스킹 필수).
- 자기승인(preparer == approver)을 허용하지 않는다.

## 필수 준수사항

- 자료 부족 시 추정 금지 → "자료 부족 / 추가 확인 필요 / 담당자 판단 필요"로 표시.
- 모든 금액 계산은 `Decimal` (float 금지). DB는 `NUMERIC`.
- 모든 AI 출력은 JSON Schema(schemas/) 검증 통과 후에만 저장. 실패 시 재처리 또는 검토 상태.
- AI 출력에는 모델 ID·프롬프트 버전·원문·처리시각을 함께 저장.
- 표본추출·계산은 재현 가능해야 한다 (seed·계산식·필터 조건 저장).
- 결정적 감사 로직(backend/app/audit_engine/)은 AI를 호출하지 않는 순수 함수로 작성.
- 상태 전이·데이터 변경은 append-only audit_trail에 기록.
- 회계기준 기본값: 일반기업회계기준(K-GAAP). 전체 중요성 기본 공식: (자산총액+매출총액)/2 × 1%.

## 정본 문서

- 자동화/승인 구분: docs/human-review-policy.md
- 감사 방법론: docs/audit-methodology.md
- 보고서 생성 정책: docs/report-generation-policy.md
- 보안 정책: docs/security-model.md
